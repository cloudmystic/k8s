# Starter pipeline
# Integration Azure DevOps pipeline with codebase in GitHub
# https://aka.ms/yaml

trigger:
  - none

stages:
    - stage: Test
      displayName: 'Test Pipeline'
      jobs:
      - job: Test
        steps:
        - script: |
            echo "**** Test Pipeline ****"
          displayName: 'Test Pipeline'
